@using RunningObjects.MVC
@using RunningObjects.MVC.Html
@model Member
@if (Model.UnderliningModel.KeyValue != null)
{
    Html.ActionLink(Model.ToString(), Model.MemberType, RunningObjectsAction.View, new {key = Model.UnderliningModel.KeyValue});
}
